🧪 Test createLogger factory

🎯 What: Improved testing for the createLogger factory function by strictly validating the exact shape of the returned logger object and ensuring proper handling of missing request context.
📊 Coverage: Added tests to verify the explicit keys and function types returned by the factory, and validated the graceful fallback for undefined context in logRequest.
✨ Result: Enhanced the reliability and strictness of the test suite, preventing regressions if the logger factory shape changes.
