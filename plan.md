1. Use `run_in_bash_session` to run `ESLINT_USE_FLAT_CONFIG=false pnpm run check && pnpm run test` to verify no functionality is broken.
2. Use `run_in_bash_session` to start the frontend server in the background, write a temporary Playwright script (`verify_cuj.py`), capture a screenshot to visually verify the frontend changes, and invoke the `frontend_verification_complete` tool. Stop background processes and clean up temporary scripts.
3. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done using `pre_commit_instructions`.
4. Use the `submit` tool to create the pull request with the branch `code-health-render-genres`, the title "🧹 Refactor renderGenres into smaller functions", and a description containing "🎯 What:", "💡 Why:", "✅ Verification:", and "✨ Result:".
